<h1>Artist: <%= @artist.name %></h1>

<div>
  <h3>Songs:</h3>
  <ul>
    <% @artist.songs.each do |song| %>
      <li>
        <%= link_to song.name, song_path(song) %>
      </li>
    <% end %>
  </ul>

</div>